import React from "react";

// styled components
import {
  HeaderView,
  HeaderTitle,
 
} from "../Styles";


const HistoryHeader = () => {
  return (
    <HeaderView>
      <HeaderTitle>Scanned Items</HeaderTitle>
    </HeaderView>
  );
};

export default HistoryHeader;