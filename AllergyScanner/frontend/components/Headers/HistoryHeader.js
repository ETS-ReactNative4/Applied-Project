import React from "react";

// styled components
import {
  HeaderView,
  HeaderTitle,
  HeaderButton,
  colors,
} from "../Styles";

// Icons

const HistoryHeader = () => {
  return (
    <HeaderView>
      <HeaderTitle>Scanned Products</HeaderTitle>
      <HeaderButton >
       
      </HeaderButton>
    </HeaderView>
  );
};

export default HistoryHeader;